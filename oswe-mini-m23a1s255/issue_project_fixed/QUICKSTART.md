# Quick Start (fixed)

## Run tests

pip install -r requirements.txt
pytest -v

## Run demo

python demo.py

All demos and tests in this fixed copy should run without raising the previous
ZeroDivisionError for the grace-period edge case.
